{
  "name": "max-space",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview --port 8080 --host"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
